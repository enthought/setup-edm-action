{
  "name": "setup-edm-action",
  "version": "1.0.0",
  "description": "GitHub Action for installing EDM",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js --license licenses.txt -o build && rm -rf dist && mv build dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/enthought/setup-edm-action"
  },
  "keywords": [
    "actions",
    "edm",
    "enthought",
    "setup"
  ],
  "author": "Enthought Inc.",
  "license": "BSD",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.0.4",
    "@actions/github": "^6.0.0",
    "expand-tilde": "^2.0.2"
  }
}
